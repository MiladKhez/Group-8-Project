In order for program to function, the song file being refrenced in the code must be downloaded. Furthermore, the song file must be  in the same pathway that program reads from (e.g. in 'MatLab' Folder).

*Note: users device must be utilizing 2 audio ports for program to function
